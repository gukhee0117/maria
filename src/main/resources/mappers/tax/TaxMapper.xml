<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.app.maria.domain.tax.mapper.TaxMapper">

    <select id="findFinalizedLotsByAccountIdsAndYear" resultType="SellLotDTO">
        select
            ke.account_id,
            so.order_id,
            so.inbound_detail_id,
            ke.final_amount,
            ke.final_at as sell_at,
            so.sell_qty,
            id.purchase_fx_rate,
            id.purchase_price,
            concat(fp.ticker, ' ', fp.name) as product_label
        from krw_exchange ke
        join sell_order so on so.order_id = ke.order_id
        join inbound_detail id on id.inbound_detail_id = so.inbound_detail_id
        join foreign_product fp on fp.foreign_product_id = id.foreign_product_id
        where ke.account_id in
        <foreach collection="accountIds" item="accountId" open="(" close=")" separator=",">
            #{accountId}
        </foreach>
            and ke.settlement_status = 'FINALIZED'
            and so.status = 'EXECUTED'
            and YEAR(ke.final_at) = #{year}
            and #{calcBaseTime} >= ke.final_at
    </select>

    <select id="findTaxRules" resultType="TaxRuleDTO">
        select
            rule_id,
            rule_type,
            rule_value,
            valid_from,
            valid_to
        from tax_rule
    </select>

    <select id="findExternalBuysByAccountIdsAndYear" resultType="ExternalBuyDTO">
        select
            a.account_id,
            tpj.trade_date,
            tpj.net_buy_amount,
            coalesce(tpj.fund_name, tpj.ticker) as product_label
        from target_product_judgement tpj
        join customer c on c.ci_hash = tpj.ci_hash
        join account a on a.customer_id = c.customer_id
        where a.account_id in
        <foreach collection="accountIds" item="accountId" open="(" close=")" separator=",">
            #{accountId}
        </foreach>
            and tpj.is_target = true
            and YEAR(tpj.trade_date) = #{year}
            and #{calcBaseTime} >= tpj.judged_at
    </select>

    <insert id="insertCalculation" parameterType="TaxCalculationDTO"
            useGeneratedKeys="true" keyProperty="calcId">
    insert into tax_calculation
    (account_id, calculated_at, basis_type, weighted_sell, original_gain_amount,
    weighted_gain, weighted_external_amount, adjust_ratio, final_deduction, final_tax)
    values
    (#{accountId}, #{calculatedAt}, #{basisType}, #{weightedSell}, #{originalGainAmount},
    #{weightedGain}, #{weightedExternalAmount}, #{adjustRatio}, #{finalDeduction}, #{finalTax})
    </insert>

    <select id="existsByAccountAndBasis" resultType="boolean">
        select exists(
            select 1
            from tax_calculation
            where account_id = #{accountId} and basis_type = #{basisType}
        )
    </select>

    <select id="findLatestCalculation" resultType="TaxCalculationDTO">
        select
            calc_id,
            account_id,
            calculated_at,
            basis_type,
            weighted_sell,
            original_gain_amount,
            weighted_gain,
            weighted_external_amount,
            adjust_ratio,
            final_deduction,
            final_tax
        from tax_calculation
        where account_id = #{accountId}
        order by calculated_at desc, calc_id desc
        limit 1
    </select>

</mapper>
